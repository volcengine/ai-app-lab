# 贡献指南

1. 创建目标明确的分支，并让改动保持在单一问题范围内。
2. 不得把真实 API Key、客户数据或受许可限制的 Provider 返回内容加入测试夹具。
3. 提交前运行 `npm run verify`；该命令会检查应用、测试初始化流程、构建生产资源并校验 Skill。
4. 涉及 Provider 解析、证据校验、日期处理或用户可见行为时，需要补充相应测试。
5. 合成测试数据必须明确标注；真实 Provider 测试必须保持为主动选择执行。

文档或示例中的金融事实必须同时提供数据截止日期和来源，否则必须明确标注为合成数据。
