# 火山引擎访问密钥
# 注意：根据新SDK，现在使用API Key进行认证
ARK_API_KEY=your_ark_api_key
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 服务配置
API_PORT=8000
API_HOST=127.0.0.1

# 模型配置
MODEL_NAME=doubao-seed-1-6-251015
VISION_MODEL_NAME=doubao-seedream-4-0-250828

# API 鉴权配置，用于验证请求是否来自合法用户
# 有效的 User Token 列表，多个 Token用逗号分隔
# 注意：用于本地测试，在生产环境中，应该将 Token 存储在安全的地方
VALID_USER_TOKENS=demo_user_token
